// Tailwind was removed in the CRA→Vite migration; no PostCSS plugins needed.
// This file exists as an empty ESM module so Vite/PostCSS auto-discovery is a
// clean no-op (renaming .js → .cjs would leave a stale reference behind).
export default { plugins: [] };
